import React from 'react';
import MainPage from '../main-page/main-page.jsx';

const App = () =>{
  return (
    <React.Fragment>
      <MainPage pageClass={`page--main`} />
    </React.Fragment>
  );
};

export default App;
